name: Node.js CI/CD

on:
  push:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install

      - name: Start application
        run: |
          node index.js &
          sleep 3

      - name: Check application
        run: curl http://localhost:3000

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker image
        run: |
          docker build \
            -t ${{ secrets.DOCKERHUB_USERNAME }}/node-ci-cd:${{ github.sha }} \
            -t ${{ secrets.DOCKERHUB_USERNAME }}/node-ci-cd:latest \
            .

      - name: Push Docker image
        run: |
          docker push \
            ${{ secrets.DOCKERHUB_USERNAME }}/node-ci-cd:${{ github.sha }}

          docker push \
            ${{ secrets.DOCKERHUB_USERNAME }}/node-ci-cd:latest

  deploy:
    runs-on: ubuntu-latest

    needs: build-and-push

    steps:
      - name: Deploy to AWS
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.AWS_HOST }}
          username: ${{ secrets.AWS_USERNAME }}
          key: ${{ secrets.AWS_SSH_KEY }}

          script: |
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/node-ci-cd:${{ github.sha }}

            docker stop node-ci-cd || true
            docker rm node-ci-cd || true

            docker run -d \
              --name node-ci-cd \
              -p 3000:3000 \
              ${{ secrets.DOCKERHUB_USERNAME }}/node-ci-cd:${{ github.sha }}
